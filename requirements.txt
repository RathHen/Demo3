Flask>=3.0.0
python-dotenv>=1.0.0
mcp>=1.0.0
keyring>=25.0.0
# Consolidated Webull SDK (provides the `webull.` namespace used by our code).
# On Windows + Python 3.13, install via install.bat which forces a prebuilt
# grpcio wheel. On Python 3.13 the SDK requests a modern grpcio that has wheels.
webull-openapi-python-sdk>=2.0.0

# Free, ~15-min-delayed option chains and quotes for strategy building.
schwab-py>=1.4.0
